# =================================================
# ARQUIVO DE PRODUÇÃO - USE SENHAS FORTES E ÚNICAS
# =================================================

# Variáveis para o serviço do PostgreSQL
POSTGRES_DB=ALTERE_PARA_POSTGRES_DB
POSTGRES_USER=ALTERE_PARA_POSTGRES_USER
POSTGRES_PASSWORD=ALTERE_PARA_POSTGRES_PASSWORD

# Variáveis para o serviço do MinIO
MINIO_ROOT_USER=ALTERE_PARA_MINIO_USER
MINIO_ROOT_PASSWORD=ALTERE_PARA_MINIO_PASSWORD
MINIO_BUCKET_NAME=ALTERE_PARA_MINIO_BUCKET

# Observação: preencha com valores seguros e únicos antes de subir em produção.
